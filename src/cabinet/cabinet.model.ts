export interface ILatestRegistration {
    firstName: string;
    secondName: string;
    iconUrl: string;
    country: string;
    createdDate: string;
}

export interface IStatistics {
    counts: any[];
}