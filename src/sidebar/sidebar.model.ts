export enum MenuItems {
    Cabinet = 0,
    Education = 1,
    Calendar = 2,
    // Promo = 3,
    Team = 3,
    // Promotion = 1,
}

const routes = ['/cabinet', '/education', '/calendar', '/statistics',];
export { routes }